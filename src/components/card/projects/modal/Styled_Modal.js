import styled from 'styled-components';

const StyledModal = styled.div`
  .modal {
    display: flex;
    width: 100%;
    height: 100%;
    background: var(--grey);
    z-index: 50;
  }
`

export default StyledModal;